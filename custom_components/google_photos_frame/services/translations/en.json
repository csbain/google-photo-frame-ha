{
  "services": {
    "sync_now": {
      "name": "Sync Now",
      "description": "Trigger immediate sync of Google Photos album"
    },
    "get_current_photo": {
      "name": "Get Current Photo",
      "description": "Get the currently displayed photo without advancing",
      "fields": {
        "entry_id": {
          "name": "Entry ID",
          "description": "Optional config entry ID (uses first available if not specified)"
        }
      }
    },
    "get_photos": {
      "name": "Get Photos",
      "description": "Get a list of all available photos in the album",
      "fields": {
        "entry_id": {
          "name": "Entry ID",
          "description": "Optional config entry ID (uses first available if not specified)"
        },
        "limit": {
          "name": "Limit",
          "description": "Maximum number of photos to return (1-500, default 100)"
        },
        "offset": {
          "name": "Offset",
          "description": "Number of photos to skip from the beginning (default 0)"
        }
      }
    },
    "next_photo": {
      "name": "Next Photo",
      "description": "Advance to the next photo and return its info. Respects shuffle mode.",
      "fields": {
        "entry_id": {
          "name": "Entry ID",
          "description": "Optional config entry ID (uses first available if not specified)"
        }
      }
    },
    "previous_photo": {
      "name": "Previous Photo",
      "description": "Go to the previous photo and return its info. Respects shuffle mode.",
      "fields": {
        "entry_id": {
          "name": "Entry ID",
          "description": "Optional config entry ID (uses first available if not specified)"
        }
      }
    },
    "set_photo": {
      "name": "Set Photo",
      "description": "Jump to a specific photo by index or ID",
      "fields": {
        "entry_id": {
          "name": "Entry ID",
          "description": "Config entry ID (required)"
        },
        "photo_index": {
          "name": "Photo Index",
          "description": "Zero-based index of the photo to display"
        },
        "photo_id": {
          "name": "Photo ID",
          "description": "ID of the photo to display (use either index or ID, not both)"
        }
      }
    },
    "clear_cache": {
      "name": "Clear Cache",
      "description": "Clear cached photos and processed images from disk",
      "fields": {
        "entry_id": {
          "name": "Entry ID",
          "description": "Optional config entry ID (clears all if not specified)"
        }
      }
    }
  }
}
